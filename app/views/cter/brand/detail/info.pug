.text-center
	if brand.post
		- post = brand.post;
		img.brandInfoImg(src=post, width="100%")
		.pcSpace(style="height:150px")
		.mbSpace(style="height:80px")
		h1.text-secondary
			.pcSpace(style="font-size: 80px;font-family:'Times New Roman';") #{brand.nome}
			.mbSpace(style="font-size: 60px;font-family:'Times New Roman';") #{brand.nome}
	else
		img.border(src=brand.logo, width="200px")
	//- .col-12.mt-2
		-website = brand.website;
		if website && website.slice(0, 4) != 'http'
			-website = 'http://'+website;
		a(href=website, target="_blank") 官网
